#ifndef __atoll_map_h
#define __atoll_map_h

#include <list>
#include <Arduino.h>

namespace Atoll {
/*
template <class K, class V, size_t S>
class Map {
   public:
    struct item {
        K key;
        V value;
    } items[S];

    bool add(K key, V value) {
        if (exists(key)) return false;
        //size_t index =
        for (size_t i = 0; i < S; i++) {
        }
    }
};
*/
}  // namespace Atoll

#endif